# 更新日志

## 2026-04-28

- 初始化前端项目，搭建文档
- 对接后端认证接口，JWT 认证，路由守卫
- 员工/部门列表：搜索、排序、分页、多选批量删除
- 顶部导航栏布局
- 首页统计卡片 + 快捷操作
- 用户管理（admin）：列表、启用/禁用、分配角色
- 薪资管理：薪资结构列表/表单、薪资记录列表/表单
- 薪资记录审核发放流程：draft → pending → approved → paid（含 rejected 驳回）
  - 状态标签展示（草稿/待审核/已审核/已驳回/已发放）
  - 按状态显示操作按钮：修改、提交审核、审核通过、审核驳回、确认发放
  - 确认发放仅 admin 可见
  - 列表新增状态筛选
  - 批量生成弹窗（全员/按部门/指定员工）
- 薪资记录编辑：仅 draft/rejected 可修改绩效系数，后端自动重算实发工资
- 考勤管理：上班/下班打卡，打卡记录（日期范围筛选），月度考勤统计（admin/manager 生成）
- 请假管理：请假申请（年假/病假/事假），请假列表状态筛选，审批（admin/manager 通过/驳回）
- 导航栏新增考勤管理、请假管理菜单

## 2026-04-28 #2

- 员工模型新增上下班时间字段（work_start_time / work_end_time）
- 员工编辑页新增上班时间/下班时间选择器
- 前端类型定义与后端 Employee 模型对齐

## 2026-04-28 #3

- 部门模型新增 parent_id 字段，支持层级树结构
- 新增 DeptTreeNode 类型定义
- 部门 API 新增 GET /depts/tree 接口
- 部门 Store 新增 fetchTree / tree / treeLoading
- 部门列表页新增"上级部门"列
- 部门表单页新增"上级部门"树形下拉选择器（编辑时排除自身及子部门防循环引用）
- 有子部门的部门不可删除

## 2026-04-28 #4

- 布局重构：顶部横向导航 → 左侧边栏 + 顶栏，侧边栏支持折叠/展开
- 薪资记录列表：新增状态列标签、状态筛选、工作流操作按钮（提交审核/审核通过/驳回/确认发放）、批量生成弹窗（全员/按部门/指定员工）
- 考勤页面重构：打卡区 + 双标签页（打卡记录/考勤统计），记录支持日期范围筛选，统计支持生成弹窗
- 请假列表：新增类型显示、状态标签列、审批按钮（admin/manager 可通过/驳回）
- 部门列表：新增描述列、上级部门列、部门树标签页（el-tree 展示层级结构）
- 部门表单：新增描述文本域、上级部门树形下拉选择器（编辑时排除自身防循环引用）

## 2026-04-29 #5

- 新增员工详情页（EmployeeDetail）：el-descriptions 展示全部字段，部门可跳转，右上角编辑按钮
- 新增部门详情页（DepartmentDetail）：基本信息 + 子部门表格 + 部门员工表格，上级部门可跳转
- 新增用户详情页（UserDetail）：基本信息 + 角色标签，右上角分配角色/启用禁用按钮
- 员工/部门/用户列表新增"查看"按钮跳转详情页
- 路由新增 /employees/:id、/departments/:id、/users/:id

## 2026-04-29 #7

- 修复 ListResult 字段名与后端不匹配：后端用 `data` 存放列表数组，前端误用 `items`，导致所有列表页空白
- ListResult 类型补充 `page`/`page_size`/`total_pages` 字段
- 员工表单：`dept_id` 改为必填（后端 `binding:"required"`），添加校验规则
- Employee 类型：`dept_id`/`department` 不再允许 null，与后端 `not null` 约束对齐

## 2026-04-29 #6

- 适配后端 #3-#7 变更：薪资审核流程、考勤请假、部门层级树、RBAC 权限、审计日志
- 登录流程：解码 JWT 提取 role 和 employee_id，存入 Pinia 全局状态
- RBAC 路由守卫：按角色控制页面访问（staff 仅考勤/请假/个人档案）
- API 拦截器：403 响应提示"权限不足"
- 薪资记录新增：status 字段展示、状态筛选、工作流操作（提交/通过/驳回/发放）、批量生成
- 薪资结构：对齐后端 employee_id + performance_factor 字段
- 考勤管理：重构为打卡区 + 双标签页（记录/统计），staff 自动填充 employee_id
- 请假管理：审批按钮（admin/manager），staff 自动填充 employee_id 并隐藏审批
- 部门新增：description、parent_id、leader_id 字段，树形视图页
- 员工新增：user_id 字段（admin 可见），个人档案页 /employees/me
- 用户新增：重置密码功能（admin），status 字段对齐后端
- 审计日志页（admin only）：操作人/实体类型/时间范围筛选，变更内容 hover 展示
- 侧边栏菜单按角色显隐：薪资/用户/审计日志 admin-only，staff 显示个人档案
- 顶栏角色标签：管理员(manager)/经理(manager)/员工(staff)
